To run this code, need an IPFS and Ganache client interfaces running in the machine
1) Open anaconda prompt
2) In prompt(cd) Navigate to ‘Block_UFullcode/blk’ folder which contains all python files
3) Run command : activate ml_env
4) Run command : python blockchain.py 
5) Run command : python manage.py runserver
